name: Multi-Job Action

on:
  push:
    branches:
      - main

jobs:
  
  job1:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install Python Packages
        run: pip install requests
  
  
  job2:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14.x'

    - name: Install Newman
      run: |
        npm install -g newman
        npm install -g newman-reporter-junitfull
        npm install -g newman-reporter-junitxray

    - name: Run Newman
      id: newman
      run: newman run CVC_Apis.postman_collection.json -r junitfull --reporter-junitfull-export result.xml || echo "::set-output name=failed::true"
      continue-on-error: true
      
    - name: Upload JUnit report
      if: steps.newman.outputs.failed == 'true'
      uses: actions/upload-artifact@v2
      with:
        name: junit-report
        path: result.xml


      
  job3:
    runs-on: ubuntu-latest
    needs: job2

    steps:
      - uses: actions/checkout@v2

      - name: Install Requests Module
        run: |
          python -m pip install requests

      - name: Run Python Script
        run: python postman_upload.py
        
      - name: Use Token in Curl Command
        env:
            TOKEN: ${{ secrets.TOKEN }}
        run: |
          curl -H 'Content-Type: text/xml' -X POST -H "Authorization': ${TOKEN}" --data @result.xml https://xray.cloud.getxray.app/api/v2/import/execution/junit?projectKey=CQ
        
        
    
